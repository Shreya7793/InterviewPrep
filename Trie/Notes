Uses:
insert(word):To insert a word
search(word): To check if String word is present or not.
startsWith(word): To check if there is any string in the Trie that starts with the given prefix string "word"

Trie{
Character a[26];
Boolean flag;
}

when boolean flag=true means word has ended.
